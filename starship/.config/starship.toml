# Starship configuration
# Docs: https://starship.rs/config/

# Left prompt — directory, git, and cloud info on line 1, then the input line
format = """
$directory$git_branch$git_status$aws$gcloud$line_break$character"""

# Right prompt — runtime/language context on the input line
right_format = """$nodejs$python$golang$rust$terraform$cmd_duration"""
